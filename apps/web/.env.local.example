# Backend URL for Next.js API proxy
BACKEND_URL=http://127.0.0.1:8000

# Comma-separated allowed dev origins (for Next.js CORS / dev server).
# Must include the http:// prefix. next.config.ts auto-adds it if missing.
DEV_ORIGINS=http://192.168.2.16:3000
